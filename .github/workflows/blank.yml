name: deploy to lambda
on: [push]
jobs:

  deploy:
    name: deploy lambda function
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: AWS Lambda Deploy
      uses: appleboy/lambda-action@master
      with:
        aws_access_key_id: AKIAURYYROFRGI66HTAO
        aws_secret_access_key: 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
        aws_region: us-east-2
        function_name: Clocker
        zip_file: output.zip
        dry_run: true
